# Frontend Environment Variables
# Copy this file to .env and fill in your values.

# Backend API base URL (only needed for production deployments)
# In development, the Vite dev-server proxy handles /api → localhost:5000
# VITE_API_URL=https://your-backend.onrender.com/api
